#include <bits/stdc++.h>
using namespace std;
using ll = long long;

#define fastio ios::sync_with_stdio(0); cin.tie(0);
#define all(v) (v).begin(),(v).end()

void solve() {
  // code
}

int main() {
  fastio

  int tc; cin >> tc;
  while (tc--) solve();

  return 0;
}