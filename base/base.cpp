#include <bits/stdc++.h>
using namespace std;
using ll = long long;

#define fastio ios::sync_with_stdio(0); cin.tie(0);
#define all(v) (v).begin(),(v).end()

int main() {
    fastio;

    // code

    return 0;
}